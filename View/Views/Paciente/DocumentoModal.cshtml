<head>
    <link rel="stylesheet" href="~/Scripts/Encaminhamento/TesteEncaminhamento.css" />
    <script src="~/Scripts/Paciente/BuscaPacienteDocScript.js"></script>
</head>

<div id="documentoModal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">

    <div class="modal-dialog modal-lg">

        <!-- Modal Content: begins -->
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">

                <div class="col-5">

                    <h4 class="modal-title" id="gridSystemModalLabel">Busca de pacientes</h4>
                </div>


            </div>

            <!-- Modal Body -->
            <div class="modal-body">
               

                <div class="row">
                    <div class="col-12">
                        <div class="body-message">
                            <span id="posto-encaminhar-texto-redirecionar" class="font-weight-bold"></span>
                            <div class="row mt-10">
                                <div class="col-6">
                                    Nome do paciente
                                </div>

                            </div>

                            <select class="form-control border-info">
                                @foreach (Model.Paciente Paciente in ViewBag.Pacientes)
                                {
                                    <option value="@Paciente.Id">@Paciente.Nome</option>

                                }
                            </select>

                            @*<select class="mdb-select" name="idPaciente" id="campo-paciente" searchable="Procure aqui...">

                                @foreach (Model.Paciente Paciente in ViewBag.Pacientes)
                                {
                                    <option value="@Paciente.Id">@Paciente.Nome</option>

                                }

                            </select>*@
                            @*<div class="row">
            <div class="col">
                <div class="form-group">
                    <input id="buscar" required="" class="form-control" type="search" />
                </div>
            </div>
        </div>*@
                        </div>
                    </div>
                </div>

            </div>

            <div class="modal-footer">
                <button id="botao" type="button" class="btn btn-info float-right">Selecionar</button>
                <button class="btn float-right" data-dismiss="modal" aria-hidden="true">Fechar</button>
            </div>
        </div>
    </div>
</div>

