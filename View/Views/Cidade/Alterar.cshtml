
@{
    ViewBag.Title = "Alterar";
}

<h2 class="h2">Alterando cidade '@ViewBag.Cidade.Nome'</h2>

<form method="post" action="/cidade/update">
    <input type="hidden" name="id" value="@ViewBag.Cidade.Id" />

    <label class="font-weight-bold" form="campo-estado">Estado<a class="text-danger">*</a></label>
    <select class="form-control border-info" name="idEstado">
        <option disabled="disabled" selected="selected">Selecione um estado</option>
        @foreach (Model.Estado estado in ViewBag.Estados)
        {
            if (estado.Id == ViewBag.Cidade.IdEstado)
            {
                <option value="@estado.Id" selected="selected">@estado.Nome</option>
            }
            else
            {
                <option value="@estado.Id">@estado.Nome</option>
            }
        }
    </select>

    <label class="font-weight-bold" for="campo-nome">Nome da Cidade<a class="text-danger">*</a></label>
    <input type="text" name="nome" class="form-control border-info" id="campo-nome" value="@ViewBag.Cidade.Nome" required="" />
           
    <button type="submit" class="btn btn-success mt-20 float-left"><i class="fa fa-save"></i> Salvar</button>
</form>

